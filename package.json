{
  "name": "mt-dev",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "dev:web": "pnpm --filter @mt-dev/web dev",
    "dev:api": "pnpm --filter @mt-dev/api dev",
    "build": "pnpm -r run build",
    "check": "pnpm -r run check",
    "deploy": "bash scripts/deploy-fast.sh",
    "deploy:fast": "bash scripts/deploy-fast.sh",
    "deploy:docker": "bash scripts/deploy-docker.sh",
    "hooks:install": "bash scripts/install-githooks.sh",
    "prepare": "vp config"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite-plus": "catalog:"
  },
  "packageManager": "pnpm@11.1.3",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "workerd"
    ]
  }
}
